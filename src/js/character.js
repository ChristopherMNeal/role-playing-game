export default class Character {
  constructor(type, skill, enemy) {
    this.type = type;
    this.skill = skill;
    this.enemy = enemy;
  }
  // getArea() {
  //   return this.side1 * this.side2;
  // }
}